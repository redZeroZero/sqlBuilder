# Testing & Coverage

## Running Tests

```bash
mvn -o test
```

## Coverage

JaCoCo is wired via `jacoco-maven-plugin`. Run the tests and open `target/site/jacoco/index.html` in each module (core, examples, integration, spring-jdbc).
